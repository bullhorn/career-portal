'use strict';

describe('Directive: CareerPortalHeader', function () {
    beforeEach(function () {
        module(function ($provide) {
            $provide.constant('configuration', {});
        });
    });

    beforeEach(module('CareerPortal'));
});
